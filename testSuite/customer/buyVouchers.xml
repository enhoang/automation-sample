<suite name="Regression Buy Vouchers">
    <!-- IMPORTANT: Before running this test flow, make sure your ENVIRONMENT is correct. -->
    <parameter name="env" value="staging"/>          <!-- staging / uat / prod -->
    <parameter name="browser" value="chrome"/>       <!-- chrome / firefox / safari -->
    <parameter name="ui.enabled" value="true"/>      <!-- true = có UI, false = headless -->
    <parameter name="wait.timeout" value="10"/>      <!-- default timeout -->
    <test name="Buy Vouchers">
        <!-- IMPORTANT: Before running this test flow, make sure your DATA/CONFIGURATION is correct. -->
        <classes>
            <class name="src.customer.a0_pages.LoginByPhone"/>
            <class name="src.customer.addVoucher.addVoucher"/>
            <class name="src.customer.addVoucher.cartVoucher"/>
            <class name="src.checkout.buy"/>
            <class name="src.checkout.addInvoice"/>
            <class name="src.checkout.getOTP"/>
            <class name="src.checkout.backVoucher"/>

        </classes>
    </test>
</suite>